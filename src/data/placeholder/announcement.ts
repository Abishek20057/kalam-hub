import type { AnnouncementContent } from "@/lib/types";

/** PHASE 1 PLACEHOLDER — replaced by site_announcements DB table in Phase 5. */
export const placeholderAnnouncement: AnnouncementContent = {
  message: "New: AI-Powered Circuit Design Assistant is Live",
  linkText: "Try Now",
  linkHref: "/projects",
  isActive: true,
  isDismissable: true,
};
